import { accountService } from './Account';
import { globalSettingsService } from './GlobalSettings';
import { authsService } from './Auth';

export const service = {
  accountService,
  globalSettingsService,
  authsService
};
